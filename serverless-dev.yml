API_URL: ${ssm:/${self:service}/${self:provider.stage}/API_URL}
MYSQL_USERNAME: ${ssm:/${self:service}/${self:provider.stage}/MYSQL_USERNAME}
MYSQL_PASSWORD: ${ssm:/${self:service}/${self:provider.stage}/MYSQL_PASSWORD}
MYSQL_HOST: ${self:custom.mySQLHost}
S3_BUCKET: ${ssm:/${self:service}/${self:provider.stage}/S3_BUCKET}
CUSTOM_AWS_ACCESS_KEY_ID: ${ssm:/${self:service}/${self:provider.stage}/CUSTOM_AWS_ACCESS_KEY_ID}
CUSTOM_AWS_SECRET_ACCESS_KEY: ${ssm:/${self:service}/${self:provider.stage}/CUSTOM_AWS_SECRET_ACCESS_KEY}
DEBUG_CONFIG: ${ssm:/${self:service}/${self:provider.stage}/DEBUG_CONFIG}
ADMIN_PASSWORD: ${ssm:/${self:service}/${self:provider.stage}/ADMIN_PASSWORD}
ONESIGNAL_KEY: ${ssm:/${self:service}/${self:provider.stage}/ONESIGNAL_KEY}
ONESIGNAL_APP: ${ssm:/${self:service}/${self:provider.stage}/ONESIGNAL_APP}
ONESIGNAL_KEY_PROVIDER: ${ssm:/${self:service}/${self:provider.stage}/ONESIGNAL_KEY_PROVIDER}
ONESIGNAL_APP_PROVIDER: ${ssm:/${self:service}/${self:provider.stage}/ONESIGNAL_APP_PROVIDER}
SENTRY_DSN: ${ssm:/${self:service}/${self:provider.stage}/SENTRY_DSN}
CMS_APP_TOKEN: ${ssm:/${self:service}/${self:provider.stage}/CMS_APP_TOKEN}
MOBILE_APP_TOKEN: ${ssm:/${self:service}/${self:provider.stage}/BOOKING_WEBSITE_APP_TOKEN}
WEBSITE_APP_TOKEN: ${ssm:/${self:service}/${self:provider.stage}/WEBSITE_APP_TOKEN}