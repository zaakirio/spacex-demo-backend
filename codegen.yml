overwrite: true
schema: "http://localhost:3000/graphql"
documents: null
useIndexSignature: true
generates:
  ./common/types/backend.ts:
    plugins:
      - "typescript"
      - "typescript-resolvers"
  ./schema.json:
    plugins:
      - "introspection"
